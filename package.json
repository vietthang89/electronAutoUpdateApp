{
  "name": "electronautoupdateapp",
  "version": "1.0.1",
  "description": "",
  "main": "app/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build  --win --publish always",
    "release": "build -p always"
  },
  "homepage": "https://github.com/vietthang89/electronAutoUpdateApp#readme",
  "devDependencies": {
    "electron": "^9.4.0",
    "electron-builder": "^21.2.0"
  },
  "dependencies": {
    "electron-updater": "^2.21.4",
    "winston": "^3.2.1"
  }
}
